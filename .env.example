# ============================================================================
# MiniFlow Enterprise - Environment Variables Example
# ============================================================================
# Bu dosyayı .env olarak kopyalayın ve değerleri doldurun:
# cp .env.example .env
#
# ÖNEMLİ: .env dosyasını git'e commit etmeyin! (.gitignore'da olmalı)
# ============================================================================

# ============================================================================
# ZORUNLU DEĞİŞKENLER
# ============================================================================

# Uygulama Ortamı
# Seçenekler: local, dev, test, prod
# Bu değer, configurations/{APP_ENV}.ini dosyasını seçer
APP_ENV=local

# Veritabanı Tipi
# Seçenekler: sqlite, postgresql, mysql
DB_TYPE=sqlite

# Configuration Dosyası Yolu (Opsiyonel)
# Belirtilmezse: ./configurations/{APP_ENV}.ini kullanılır
# CONFIG_PATH=./configurations/local.ini

# Test Validation Key (ZORUNLU)
# EnvironmentHandler bu key'i kontrol eder
# Değer: "ThisKeyIsForConfigTest" olmalı
TEST_KEY=ThisKeyIsForConfigTest

# JWT Secret Key (ZORUNLU)
# JWT token'ları imzalamak için kullanılır
# Minimum 32 karakter olmalı
# Örnek: openssl rand -hex 32
JWT_SECRET_KEY=your_jwt_secret_key_minimum_32_characters_long

# JWT Algorithm (Opsiyonel)
# Default: HS256
# JWT_ALGORITHM=HS256

# Encryption Key (ZORUNLU)
# Veri şifreleme için kullanılır (credentials, variables, vb.)
# Minimum 32 karakter olmalı
# Örnek: openssl rand -hex 32
ENCRYPTION_KEY=your_encryption_key_minimum_32_characters_long

# ============================================================================
# OPSİYONEL DEĞİŞKENLER
# ============================================================================

# ============================================================================
# Redis Configuration (Opsiyonel)
# ============================================================================
# Redis, rate limiting ve session yönetimi için kullanılır
# Development için opsiyonel, production için önerilir

# Redis Host
# Default: localhost
# REDIS_HOST=localhost

# Redis Port
# Default: 6379
# REDIS_PORT=6379

# Redis Password (Opsiyonel)
# REDIS_PASSWORD=

# Redis Database Number
# Default: 0
# REDIS_DB=0

# ============================================================================
# Mailtrap Configuration (Opsiyonel)
# ============================================================================
# Email gönderimi için Mailtrap servisi kullanılır
# Development ve test için önerilir

# Mailtrap API Key
# Mailtrap hesabınızdan alabilirsiniz: https://mailtrap.io/
# MAILTRAP_API_KEY=your_mailtrap_api_key

# Mailtrap Sender Email (Opsiyonel)
# Default: configurations/{APP_ENV}.ini dosyasındaki [Mailtrap] bölümünden alınır
# MAILTRAP_SENDER_EMAIL=noreply@example.com

# ============================================================================
# Database Configuration (Opsiyonel)
# ============================================================================
# SQLite kullanıyorsanız bu değişkenler gerekli değil
# PostgreSQL veya MySQL kullanıyorsanız configurations/{APP_ENV}.ini dosyasında tanımlayın

# PostgreSQL/MySQL için (configurations/{APP_ENV}.ini dosyasında tanımlanır):
# [Database]
# db_host=localhost
# db_port=5432
# db_user=postgres
# db_password=your_password
# db_name=miniflow

# ============================================================================
# Production Ortamı İçin Ek Notlar
# ============================================================================
#
# Production ortamında:
# 1. Tüm secret key'leri güvenli bir şekilde saklayın (secret manager, vb.)
# 2. APP_ENV=prod kullanın
# 3. PostgreSQL veya MySQL kullanın (SQLite production için önerilmez)
# 4. Redis kullanın (rate limiting için kritik)
# 5. HTTPS kullanın
# 6. CORS ayarlarını production domain'lerine göre yapılandırın
# 7. Rate limiting değerlerini production yüküne göre ayarlayın
#
# ============================================================================

# ============================================================================
# Hızlı Başlangıç
# ============================================================================
#
# 1. Bu dosyayı .env olarak kopyalayın:
#    cp .env.example .env
#
# 2. Zorunlu değişkenleri doldurun:
#    - APP_ENV=local
#    - DB_TYPE=sqlite
#    - TEST_KEY=ThisKeyIsForConfigTest
#    - JWT_SECRET_KEY=... (32+ karakter)
#    - ENCRYPTION_KEY=... (32+ karakter)
#
# 3. Opsiyonel değişkenleri ihtiyacınıza göre ayarlayın
#
# 4. Setup komutunu çalıştırın:
#    python -m src.miniflow setup
#
# 5. Uygulamayı başlatın:
#    python -m src.miniflow run
#
# ============================================================================
