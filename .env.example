# =============================================================================
# MiniFlow Enterprise - Environment Configuration
# =============================================================================
# Kurulum:
#   1. cp .env.example .env
#   2. Secret key'leri oluştur: python -c "import secrets; print(secrets.token_hex(32))"
#   3. Oluşan değerleri JWT_SECRET_KEY ve ENCRYPTION_KEY'e yapıştır
# =============================================================================

# Uygulama Ortamı (local, dev, test, prod)
APP_ENV=local

# Veritabanı Tipi (sqlite, postgresql, mysql)
DB_TYPE=sqlite

# Validasyon Key (DEĞİŞTİRME!)
TEST_KEY=ThisKeyIsForConfigTest

# JWT Ayarları (ZORUNLU - secrets.token_hex(32) ile oluştur)
JWT_SECRET_KEY=BURAYA_OLUSTURULAN_KEY_YAPISTIR
JWT_ALGORITHM=HS256

# Şifreleme Key (ZORUNLU - secrets.token_hex(32) ile oluştur)
ENCRYPTION_KEY=BURAYA_OLUSTURULAN_KEY_YAPISTIR

# Redis (opsiyonel - local development için)
# REDIS_HOST=localhost
# REDIS_PORT=6379

# Mailtrap (opsiyonel - email gönderimi için)
# MAILTRAP_API_KEY=your_api_key
